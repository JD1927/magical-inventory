meta {
  name: Create Category
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "name": "Magic Wands",
    "description": "Various types of wands for magical purposes.",
    "isMain": true
  }
}
